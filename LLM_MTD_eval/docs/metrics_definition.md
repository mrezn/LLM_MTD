# Metrics Definition

Phase 1 records four metric groups:

- QoS metrics: latency, throughput, queue, message loss
- Security metrics: path visibility and attack/defense outcome flags
- Overhead metrics: controller timing and rule-install counts
- LLM quality metrics: valid JSON rate, fallback use, unsupported-action rate,
  decision latency
